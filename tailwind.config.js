module.exports = {
  // ... other config
  theme: {
    extend: {
      animation: {
        "spin-slow": "spin 8s linear infinite",
        "spin-slower": "spin 12s linear infinite reverse",
      },
    },
  },
};
